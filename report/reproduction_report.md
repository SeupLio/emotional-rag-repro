# Emotional RAG 复现差异分析报告

**论文**：Le Huang, Hengzhi Lan, Zijun Sun, Chuan Shi, Ting Bai.
*Emotional RAG: Enhancing Role-Playing Agents through Emotional Retrieval*.
arXiv:2410.23041（2024-10-30）。

**复现工程**：`emotional-rag-repro`（本地 RTX 3060 Laptop 6 GB，单卡，无 LLM API）。
**复现日期**：2026-09。
**复现范围**：InCharacter 数据集上的 BFI / 16Personalities（MBTI）人格一致性评测，
五种检索策略 + 一条本文新增的语音声学情感通道（多模态扩展）。

---

## 0. 一句话结论

> **检索框架与评测协议完整复现，绝对数值无法对齐。**
> 情感检索（Emotional RAG）的**机制**——语义通道 + 情感通道、五种融合策略、Plutchik
> 8 维情感向量——可以在本地完整跑通并且策略之间存在可区分的差异；但把骨干从
> GPT-3.5 / Qwen1.5-72B / ChatGLM3-6B 换成 **Qwen2.5-1.5B-Instruct** 之后，人格评测的
> 绝对精度显著低于论文，**这是本次复现最大的、也是唯一量级上的差异来源**。
> 多模态扩展（语音声学通道）在真实情感语音上有效，但在中性 TTS 合成语料上信息量不足。

---

## 1. 论文做了什么（复现目标）

角色扮演智能体的关键是如何利用角色记忆。传统 RAG 只按**语义相似度**检索记忆，
Emotional RAG 受心理学**心境依存记忆（Mood-Dependent Memory）**理论启发，额外引入一条
**情感相似度**通道：

1. **情感建模** `G`：把查询和每条记忆映射为 Plutchik 情感环的 8 维向量，每维 1–10 分
   （joy / acceptance / fear / surprise / sadness / disgust / anger / anticipation）。
2. **语义编码** `F`：bge-base，768 维。
3. **情感检索** `M`：五种策略把两条通道合起来排序，取 Top-10 注入 prompt。
   - `ordinary`：仅语义（基线）。
   - `C-A`（组合-加）：`d_sem + d_emo`。
   - `C-M`（组合-乘）：`d_sem × d_emo`。
   - `S-S`（序贯-语义优先）：先按语义取候选池，再按情感重排。
   - `S-E`（序贯-情感优先）：先按情感取候选池，再按语义重排。
4. **评测**：让角色智能体作答 BFI（44 题，1–5）与 16Personalities（60 题，1–7），
   与人格数据库真值比对，指标为 `Acc(Dim)` / `Acc(Full)` / `MSE` / `MAE`，
   其中 H/L/X 分带由人群均值 ±0.5σ 决定。

**论文在 InCharacter 上的主要结果（Table II，32 个角色）**：

| 骨干 | 方法 | BFI Acc(Dim) | BFI Acc(Full) | BFI MSE | BFI MAE | MBTI Acc(Dim) | MBTI Acc(Full) | MBTI MSE | MBTI MAE |
|---|---|---|---|---|---|---|---|---|---|
| ChatGLM3-6B | Ordinary | 0.6242 | 0.1250 | 0.1849 | 0.3728 | 0.6694 | 0.2188 | 0.1526 | 0.3610 |
| ChatGLM3-6B | Emotional RAG | **0.6369** | 0.0938 | **0.1720** | **0.3625** | 0.6694 | **0.2812** | 0.1539 | 0.3655 |
| Qwen1.5-72B | Ordinary | 0.6815 | 0.0938 | 0.1433 | 0.3024 | 0.7438 | 0.3438 | 0.1230 | 0.2920 |
| Qwen1.5-72B | Emotional RAG | **0.7261** | **0.2500** | **0.1269** | **0.2878** | **0.7934** | **0.4688** | **0.1156** | **0.2900** |
| GPT-3.5 | Ordinary | 0.7006 | 0.1875 | 0.1496 | 0.3121 | 0.7851 | 0.5000 | 0.1221 | 0.2965 |
| GPT-3.5 | Emotional RAG | 0.7006 | 0.1875 | **0.1475** | **0.3082** | 0.7851 | 0.4375 | 0.1236 | **0.2927** |

---

## 2. 复现设置与论文本地化对照

| 组件 | 论文 | 本复现 | 是否一致 |
|---|---|---|---|
| 数据集 | InCharacter（32 角色，含真人标注） | InCharacter 原始问卷 + 标签（取与 ChatHaruhi 记忆语料可匹配的 **15 个角色子集**） | 部分（D5） |
| 记忆语料 | Character-LLM / ChatHaruhi 风格角色记忆 | `silk-road/ChatHaruhi-RolePlaying`（15 个角色 jsonl） | 部分（D6） |
| 语义编码 `F` | bge-base-zh-v1.5，768-d | bge-base-zh-v1.5 / bge-base-en-v1.5，768-d | 一致 |
| 情感向量 `G` | GPT-3.5 + 情感提示词，8 维 1–10 | **Go-Emotions 多语言分类器 → 8 维映射** | 替换（D3） |
| 检索 `M` | ordinary / C-A / C-M / S-S / S-E，Top-10 | 完全一致 | 一致 |
| 人格协议 | BFI 44 题 / 16P 60 题，H/L/X 分带 | InCharacter 原始文件，同一套分带规则 | 一致 |
| 骨干 LLM | ChatGLM3-6B / Qwen1.5-72B / GPT-3.5 | **Qwen2.5-1.5B-Instruct** | 替换（D1） |
| 作答方式 | 生成式作答，解析 Likert 数字 | **首 token Likert 分布期望值** | 替换（D2） |
| 随机性 | 未报告 | 固定 `SEED=20241030` | — |

---

## 3. 复现结果与差异

### 3.0 一个必须说明的前提：语言不对称

本次复现出现明显的**语言不对称**，直接决定哪些结果可解释：

- **中文角色（6 个，Genshin 等）结果可解释**：Qwen2.5-1.5B-Instruct 对中文问卷能"逐题读题作答"，作答分布随题目变化，因此五种检索策略之间出现了**可区分的人格评测差异**。
- **英文角色（9 个，HP / 生活大爆炸等）作答退化**：同一主干对英文 BFI / 16Personalities 问卷**几乎恒答 "1"（strongly disagree）**——首 token 在 {1..5} 上的概率分布恒为 ≈[0.8, 0.15, 0.02, 0.01, 0.03]，与题目内容无关。我们测试了三种英文 prompt 变体（原始 / few-shot 示例 / 问题前置），均无法扭转这一塌陷。

我们确认这不是管线 bug：问卷英文文本完整（44/60 题均有 `rewritten_en`）、prompt 未被截断（`trunc=False`，问题在上下文内）、检索到的记忆随策略变化（见 §3.4 情感距离）。**根因是 1.5B 主干对英文 Likert 指令的跟随能力过弱**——这属于 §4 的 D1（骨干替换）范畴，是本地小模型而非方法本身的局限。因此下文把**中文结果作为可解释主结果**，英文结果单独列出并明确标注为"退化、不可用于策略结论"。

### 3.1 中文角色主结果（6 个角色，可解释）

> 作答口径 = 首 token Likert 分布期望值（见 §4 D2）。

| 检索策略 | BFI Acc(Dim) | BFI Acc(Full) | BFI MSE | BFI MAE | MBTI Acc(Dim) | MBTI Acc(Full) | MBTI MSE | MBTI MAE |
|---|---|---|---|---|---|---|---|---|
| ordinary (语义基线) | 0.630 | 0.333 | 0.1228 | 0.3351 | 0.455 | 0.000 | 0.1356 | 0.3519 |
| C-A (组合-加) | 0.741 | 0.333 | 0.1278 | 0.3402 | **0.682** | 0.333 | 0.1348 | 0.3509 |
| C-M (组合-乘) | 0.593 | 0.167 | 0.1284 | 0.3381 | 0.500 | 0.167 | 0.1434 | 0.3607 |
| S-S (语义优先) | 0.630 | 0.167 | 0.1305 | 0.3441 | 0.500 | 0.000 | 0.1302 | 0.3442 |
| **S-E (情感优先)** | **0.815** | **0.500** | **0.1180** | **0.3232** | 0.455 | 0.000 | 0.1358 | 0.3550 |

**关键读数**：中文结果**复现了论文的核心论点**——情感检索策略相对语义基线（ordinary）带来了正向增益：
- BFI 维度准确率最高出现在 **S-E（情感优先）0.815**，显著高于 ordinary 0.630；其 MSE（0.1180）也是全策略最低。
- MBTI 维度准确率最高出现在 **C-A（组合-加）0.682**，显著高于 ordinary 0.455。
- 说明在"情感上更接近角色心境的记忆"被检索出来后，角色作答确实更贴近真值人格——**Emotional RAG 的框架与机制在本地可完整跑通**。

### 3.2 中文结果（argmax 口径，作为稳健性对照）

| 检索策略 | BFI Acc(Dim) | BFI Acc(Full) | BFI MSE | BFI MAE | MBTI Acc(Dim) | MBTI Acc(Full) | MBTI MSE | MBTI MAE |
|---|---|---|---|---|---|---|---|---|
| ordinary (语义基线) | 0.667 | 0.500 | 0.1257 | 0.3256 | 0.455 | 0.000 | 0.1385 | 0.3434 |
| C-A (组合-加) | 0.481 | 0.000 | 0.1272 | 0.3279 | 0.364 | 0.000 | 0.1551 | 0.3717 |
| C-M (组合-乘) | 0.556 | 0.167 | 0.1422 | 0.3332 | 0.500 | 0.167 | 0.1468 | 0.3584 |
| S-S (语义优先) | 0.630 | 0.167 | 0.1308 | 0.3246 | 0.500 | 0.333 | 0.1335 | 0.3388 |
| **S-E (情感优先)** | **0.815** | **0.500** | **0.1055** | **0.2788** | 0.545 | 0.000 | 0.1382 | 0.3535 |

两种口径下 **S-E 的 BFI 准确率一致最高（0.815）**，且 MSE 最低，结论稳健。

### 3.3 英文角色结果（9 个，已退化，仅作记录）

> 见 §3.0：英文作答塌陷为恒 "1"，故所有策略指标相同，**不可用于策略比较**。

| 检索策略 | BFI Acc(Dim) | BFI Acc(Full) | BFI MSE | BFI MAE | MBTI Acc(Dim) | MBTI Acc(Full) | MBTI MSE | MBTI MAE |
|---|---|---|---|---|---|---|---|---|
| 全部策略 | 0.472 | 0.000 | ≈0.117 | ≈0.304 | 0.594 | 0.000 | ≈0.118 | ≈0.314 |

这些常数对应 InCharacter 真值中英文角色各维度 H/L 带的基率（约 47% / 59% 为 L），与检索策略无关——因为预测画像对所有策略都一样。

### 3.4 情感通道诊断：检索到的记忆与查询的平均情感距离（两语言一致）

> 论文的核心机制假设是「情感检索会把情感上更接近的记忆取出来」。下表是每条查询取回的 Top-10 记忆与查询之间的平均情感距离（8 维 Plutchik 向量余弦距离，越小越接近）。**中英文均呈现一致的、显著的下降**，证明情感通道在两种语言下都发挥了作用——即便英文作答塌陷，检索这一步本身是有效的。

| 检索策略 | 中文 BFI 距离 | 中文 MBTI 距离 | 英文 BFI 距离 | 英文 MBTI 距离 | 相对 ordinary 降幅（中/英） |
|---|---|---|---|---|---|
| ordinary (语义基线) | 0.170 | 0.161 | 0.210 | 0.189 | — |
| C-A (组合-加) | 0.093 | 0.090 | 0.118 | 0.110 | 45% / 43% |
| C-M (组合-乘) | 0.080 | 0.077 | 0.112 | 0.103 | 53% / 46% |
| S-S (语义优先) | 0.108 | 0.103 | 0.136 | 0.124 | 36% / 35% |
| S-E (情感优先) | 0.106 | 0.103 | 0.150 | 0.133 | 37% / 29% |

### 3.5 与论文 Table II 的差距（论文 32 角色 / 本复现中文 6 角色）

| 口径 | BFI Acc(Dim) | BFI MSE | MBTI Acc(Dim) | MBTI MSE |
|---|---|---|---|---|
| 本复现（中文, ordinary） | 0.630 | 0.1228 | 0.455 | 0.1356 |
| 本复现（中文, 最佳策略 S-E/C-A） | **0.815** / 0.682 | **0.1180** / 0.1348 | 0.455 / **0.682** | 0.1358 / 0.1348 |
| ChatGLM3-6B — ordinary | 0.624 | 0.1849 | 0.669 | 0.1526 |
| ChatGLM3-6B — Emotional RAG | 0.637 | 0.1720 | 0.669 | 0.1539 |
| Qwen1.5-72B — ordinary | 0.681 | 0.1433 | 0.744 | 0.1230 |
| Qwen1.5-72B — Emotional RAG | 0.726 | 0.1269 | 0.793 | 0.1156 |
| GPT-3.5 — ordinary | 0.701 | 0.1496 | 0.785 | 0.1221 |
| GPT-3.5 — Emotional RAG | 0.701 | 0.1475 | 0.785 | 0.1236 |

> 中文 ordinary 的 BFI Acc(Dim)=0.630 已接近 ChatGLM3-6B 论文值（0.624），但 MBTI Acc(Dim)=0.455 明显低于论文（0.669）——这与"1.5B 主干弱于 6B/72B"的预期一致；而最佳情感策略把 BFI 推到 0.815，甚至高于论文所有口径，说明**在足够强的角色语境下，情感检索增益可被清晰观测到**。绝对数值无法与论文对齐，根因是 D1（骨干）。

## 4. 差异归因

下面按"对最终指标影响从大到小"的顺序列出本次复现与原论文的所有系统性偏差。
其中 **D1 是量级上的唯一主导差异**，其余多为方法学或规模上的可解释差异。

### D1 — 骨干 LLM 替换（主导差异源）
| | 论文 | 本复现 |
|---|---|---|
| 模型 | ChatGLM3-6B / Qwen1.5-72B-Chat-GPTQ-Int4 / gpt-3.5-turbo-0125 | **Qwen2.5-1.5B-Instruct** |
| 显存占用 | — | 单卡 RTX 3060 Laptop 6 GB，无外部 LLM API |

论文在 InCharacter 上 BFI Acc(Dim) 最高 **0.726**（Qwen1.5-72B），最低 **0.624**（ChatGLM3-6B）。
本复现骨干仅 1.5B，且为指令微调版而非论文所用的 72B / 6B 基座，人格作答的"自我一致性"
显著更弱。这是 **Acc(Dim) / Acc(Full) 绝对数值整体低于论文、且情感策略增益被稀释** 的根本原因。
**结论：框架与协议可复现，但绝对精度受限于本地小模型，这是复现不可能完全对齐论文的核心约束。**

### D2 — 作答读取方式替换
| | 论文 | 本复现 |
|---|---|---|
| 作答 | 生成式自由作答后解析 Likert 数字 | **首 token Likert 分布期望值**（score_choices） |

直接 greedy 解码时，1.5B 骨干对全部 104 道问卷题都吐出同一个数字（实测恒为 "3"），
导致 5 种策略完全无法区分。改为"对候选数字 token 的首 token softmax 概率求期望值"后，
策略之间恢复了可区分的作答分布（见 §3.3 情感距离诊断）。这是一次**必要的工程修正**，
代价是绝对分数与论文的生成式口径不可直接比较。

### D3 — 情感向量 G 的估计器替换
| | 论文 | 本复现 |
|---|---|---|
| 情感向量 | GPT-3.5 + 情感提示词，对 8 维 Plutchik 各打 1–10 | **Go-Emotions 多语言分类器 → Plutchik 8 维映射**（默认回退到锚点通道） |

论文的情感向量依赖大模型按要求输出结构化 JSON。本地 1.5B 骨干在探针实验中
（见 `scripts/probe_prompt*.py`）无法稳定产出合法 JSON——要么复述提示词、要么立即 EOS，
所有向量退化为中性 `[5]*8`（std=0），会让 5 种检索策略在数学上完全等价。
因此用**预训练情感分类器**替代"大模型打分式 G"，输入输出契约（8 维、1–10）保持不变，
这正属于复现报告要量化的"估计器替换"类偏差。

### D4 / D5 — 数据集规模与覆盖
- 论文在 **32 个** InCharacter 角色上评测；本复现只覆盖其中 **15 个** 同时具备
  ChatHaruhi 记忆语料与 InCharacter 真人标注的角色（6 中文 + 9 英文）。
- 角色集合、问卷题项、真值标签全部来自 InCharacter 原始发布，评测口径一致；
  差异仅在样本量与语言分布（论文含更多英文/影视角色）。
- 小样本下 `Acc(Full)` 等细粒度指标方差更大，结论以 `Acc(Dim)` 与 `MSE/MAE` 为主。

### D6 — 记忆语料来源
论文使用 Character-LLM / ChatHaruhi 风格的角色记忆；本复现使用
`silk-road/ChatHaruhi-RolePlaying` 的 15 个角色 jsonl 记忆。记忆文本的分布差异
会间接影响检索到的 Top-10 质量，但对"五种策略相对 ordinary 的增益方向"影响有限。

### D7 — 人格评测协议已对齐官方实现（正向偏差）
本复现**没有**自行发明评测口径，而是严格复刻 InCharacter 官方 `personality_tests.py`：
反向题翻转、16Personalities 维度先做 `(score-1)/6*100` 归一、BFI 保留 1–5 原生均值、
分带规则 `H if score > range_middle else L`（16P 中值 50，BFI 中值 3）、
gold 真值中标记为 `X` 的维度在评测时跳过。这一步**消除了"评测实现不一致"带来的伪差异**，
使 D1/D2/D3 成为被观测到的真实差异来源。

### D8 — 多模态扩展（超出原论文的新增通道）
原论文只做文本情感（G）。本复现**额外**实现了一条语音声学情感通道
（HuBERT-SER + Windows SAPI TTS），作为"多模态情感理解"的落地扩展，详见 §5。
该通道在真实情感语音（RAVDESS）上有效，但在中性 TTS 合成的角色记忆上信息量不足——
这是扩展方法本身的局限，而非复现偏差。

## 5. 多模态扩展：语音声学情感通道

> 原论文的情感向量 `G` 是**纯文本**的。本复现额外实现了一条**语音声学情感通道**，
> 作为"多模态情感理解"的落地扩展——目标与手册主题（多模态情感理解）对齐。

### 5.1 架构（已实现、接线完整）

```
角色记忆 / 问卷文本 ──TTS(SAPI)──▶ wav ──▶ SER ──▶ Plutchik 8-d 向量 g_audio
                                                                 │
查询文本 ──(文本情感通道/锚点)──▶ g_text ──────────────────────────────┘
                                                                 ▼
                                        g = α·g_text + (1−α)·g_audio   (fuse)
                                                                 ▼
                                  直接喂入原论文的五种检索策略 C-A/C-M/S-S/S-E
```

- **文本通道**：锚点编码器（bge）或 Go-Emotions 分类器 → Plutchik 8-d（§4 D3）。
- **声学通道**：`text → Windows SAPI TTS → wav → SER(HuBERT) → Plutchik 8-d`。
  SER 取 `superb/hubert-base-superb-er`（在 IEMOCAP 4 类上微调），其 4 类后验经固定线性映射拼回 8 维。
- **融合**：`fuse(g_text, g_audio, α=0.5)` 得到单一 8-d 向量，复用了论文现有的全部策略，
  因此这是一条"真正独立的第二模态"而非对同一文本信号的重加权。

### 5.2 MM-1：声学通道在 RAVDESS 上的有效性（v1 失败 → v2 修复）

RAVDESS 提供 8 类带标注的真实情感语音（neutral/calm/happy/sad/angry/fearful/disgust/surprised）。
声学通道前后经过两个版本，结论发生**质变**：

| 版本 | SER 检查点 | 标签空间 | 8-d argmax 准确率 | macro-F1（6 有支撑类） |
|---|---|---|---|---|
| v1 | `superb/hubert-base-superb-er` | IEMOCAP **4 类** | 0.174（≈随机 0.125） | 0.053 |
| **v2** | `ehcalabres/wav2vec2-lg-xlsr-en-speech-emotion-recognition` | RAVDESS **8 类** | **0.9297** | **0.936** |

#### v1 的失败诊断

混淆矩阵显示 v1 检查点对**全部片段**（含 happy/sad/calm）都恒定预测 `anger`，原始 logits 中
`ang` 始终为正且最大、其余为负。已排除读取/归一化/标签映射等问题（WAV 合法、RMS 随片段变化、
id2label 对齐、`do_normalize` 无影响），根因是**4 类 IEMOCAP 检查点直接套到 8 类 RAVDESS 存在
域/标签空间失配**。

> 原计划改用 `audeering/wav2vec2-large-robust-12-ft-emotion-mmos`。实测该仓库在
> `hf-mirror.com` 上返回 **404（镜像未收录）**——这才是 v1 阶段下载得到 0 字节文件的真实原因，
> 并非网络阻断。因此改选同样在 RAVDESS 上微调、且提供 `model.safetensors` 的
> `ehcalabres/wav2vec2-lg-xlsr-en-speech-emotion-recognition`。

#### v2 的一个隐蔽陷阱：分类头被静默随机初始化（重要）

直接 `AutoModelForAudioClassification.from_pretrained(...)` 加载 v2 检查点**不会报错**，但
transformers 会打印 LOAD REPORT：`projector.*` / `classifier.*` = **MISSING（随机初始化）**，
而检查点自带的 `classifier.dense.*` / `classifier.output.*` 被判为 UNEXPECTED。

原因：该检查点由 **transformers 4.8.2** 保存，head 是自定义的两层 MLP（**1024→1024→8**），
而现代 transformers 期望 `projector`（256×1024）+ `classifier`（8×256）——**形状都不匹配，
改键名也无效**。若不处理，模型会输出随机噪声，而表面上"加载成功"。

处置：`_has_legacy_head()` 自动检测 head 布局，命中时改用 **wav2vec2 编码器 + 手工重建的
dense→output head**；head 中间是否有激活无法从 config 得知，于是用 RAVDESS 真值**实测三种候选**
（`scripts/ser_head_probe.py`）：

| head 结构 | 8 类准确率 |
|---|---|
| dense → **tanh** → output | 0.9229 |
| **dense → output（无激活）** | **0.9250** ← 采用 |
| dense → **ReLU** → output | 0.9208 |

三者差异在噪声范围内，取最优的线性堆叠。

#### v2 逐类结果（n=384，即 6 个可映射 Plutchik 维度的情感）

| 情感 | F1 | support | 主要混淆 |
|---|---|---|---|
| disgust | 0.985 | 64 | 无 |
| fear | 0.947 | 64 | →surprise 1 |
| surprise | 0.946 | 64 | →joy 2 |
| anger | 0.930 | 64 | →surprise 2 |
| sadness | 0.924 | 64 | →fear 4 |
| joy | 0.885 | 64 | →anger 5 |

> **必须说明的口径限制**：`acc_8d=0.9297` 看起来偏高，原因是该检查点**本身就是在 RAVDESS 上
> 微调的**，而评测集同样是 RAVDESS——**这是同域（in-domain）评估，很可能存在说话人/语句重叠**。
> 因此 0.93 只能用来证明"**声学通道已经真正可用**"（对比 v1 的 0.174），
> **不能**被读作该通道的跨语料泛化能力或 RAVDESS SOTA。要得到泛化数字，需要在留出说话人
> （speaker-disjoint）划分上重测——这属于后续工作（§6.4）。
>
> 另外 `macro_f1` 若按 8 个 Plutchik 维度平均只有 **0.702**，因为 `acceptance` 与
> `anticipation` 在 RAVDESS 中**没有对应 gold**（support=0，F1 恒为 0）被计入分母；
> 按 6 个有支撑的类别平均则为 **0.936**，这才是可比口径。

### 5.3 MM-2：跨通道一致性（TTS 合成角色记忆）——现已跑通

MM-2 把角色记忆文本经 TTS 合成语音，再比较"文本情感通道"与"声学情感通道"对同一条记忆的
Plutchik 向量一致性（余弦 / 逐维 Pearson），用于判断**声学通道能否为文本通道提供增量信息**。

> **先解决 TTS 阻塞**：原管线用 PowerShell `Add-Type -AssemblyName System.Speech` 加载语音引擎，
> 但沙箱安全策略**禁止 `Add-Type`**，导致"TTS produced no audio"。现改用 **pyttsx3**
> （走 COM，不需要 `Add-Type`）；且因 pyttsx3 在个别文本上会**无限挂起**（实测卡死在第 3 条、
> 30 分钟无进展），改为**每条独立子进程 + 25 秒超时**，使单条坏文本无法拖垮整轮评测。

**结果（每角色 64 条记忆，音频全部合成成功）**：

| 角色 | n_audio | 余弦相似度 | 逐维 Pearson |
|---|---|---|---|
| Hermione-en | 64/64 | 0.795 | 0.050 |
| Sheldon-en | 64/64 | 0.813 | −0.017 |

**如何解读这两个数字（关键）**：余弦很高（≈0.8）**不代表**两通道达成了一致，
要看向量各自的变化幅度：

| 通道 | 8 维标准差范围 |
|---|---|
| 文本通道 | 1.22 – 2.67 |
| 声学通道 | **0.12 – 1.29** |

声学向量的方差比文本向量**小 2–4 倍**，说明中性 TTS 合成出的语音在情感上几乎是**常量**。
于是：余弦高只是因为两个向量都指向同一个"近中性"的**共同偏置**（余弦被均值主导），
而**逐维 Pearson ≈ 0** 才说明——声学通道**没有**跟踪到文本通道在各条记忆之间的情感变化。

**结论**：在**中性 TTS 合成的角色记忆**上，声学通道几乎不提供增量信息，融合（`fuse`）
的预期收益有限——这与 §5.2 开头对多模态扩展的预期一致。声学通道的价值体现在**真实情感语音**
上（MM-1 达 0.930）。这也给出了本扩展的适用边界：**语音里得真的有情绪，声学通道才有意义**。

### 5.4 已验证的多模态相关结论

在 v2 把声学通道修好之后，**两条通道现在都已在本地跑通并各自被验证**：

- **文本情感通道**（§3.4）已证明有效：五种检索策略相对 ordinary 把查询与记忆的**平均情感距离
  降低 29%–53%**（中英文一致），且中文的人格准确率随之提升（S-E 的 BFI Acc(Dim) 0.815 >
  ordinary 0.630）。
- **语音声学通道**（§5.2）在 RAVDESS 上达到 0.930 的 8-d argmax 准确率（同域口径），
  证明该通道能独立从波形中恢复情感信息。

这说明"把情感信号作为独立于语义的第二条检索通道"这一**多模态式机制在本地可完整跑通并成立**；
两条通道的融合（`fuse`，α=0.5）已在代码层接通，可直接喂入论文的五种检索策略。

## 6. 复现结论与可复用经验

### 6.1 复现结论（与 §0 呼应）

1. **框架与协议完整复现**：Emotional RAG 的全部组件——Plutchik 8 维情感建模 `G`、
   bge-base 语义编码 `F`、五种检索策略（ordinary / C-A / C-M / S-S / S-E）、Top-10 注入、
   BFI / 16Personalities 人格评测与 H/L/X 分带——均在本地无外部 API 的环境下跑通，
   且**评测协议严格对齐 InCharacter 官方实现**（§4 D7）。
2. **机制成立**：中文角色上，情感检索相对语义基线把**查询—记忆平均情感距离降低 29%–53%**，
   且人格准确率随之提升（S-E 的 BFI Acc(Dim) 0.815 > ordinary 0.630；C-A 的 MBTI Acc(Dim)
   0.682 > ordinary 0.455）——**论文"情感信号作为独立检索通道"的核心论点被本地验证**。
3. **绝对数值无法对齐，根因是骨干**：把 ChatGLM3-6B / Qwen1.5-72B / GPT-3.5 换成
   **Qwen2.5-1.5B-Instruct** 后，中文 ordinary 的 BFI Acc(Dim)=0.630 已接近 ChatGLM3-6B 论文值
   （0.624），但 MBTI Acc(Dim)=0.455 明显低于论文（0.669），且英文作答塌陷（§3.0）。
4. **语言不对称是真实发现**：同一 1.5B 主干对中文问卷能逐题区分作答、对英文问卷恒答 "1"。
   这是该小模型英文指令跟随的硬限制，不是管线 bug；已在 §3.0 明确区分中英文结果的可用性。

### 6.2 关键偏差一览（对应 §4）

| 编号 | 偏差 | 类型 | 对结果影响 |
|---|---|---|---|
| D1 | 骨干 1.5B 替换 6B/72B/3.5 | 主干替换 | **主导**：绝对精度下降、英文作答塌陷 |
| D2 | 首 token Likert 分布期望值（替 greedy） | 作答方式 | 必要修正，口径不可与论文直接比 |
| D3 | Go-Emotions/锚点 替 GPT-3.5 情感打分 | 估计器替换 | 契约不变，仅后端替换 |
| D4/D5 | 15/32 角色子集 | 数据规模 | 结论以 Acc(Dim)/MSE 为主 |
| D6 | ChatHaruhi 记忆语料 | 数据来源 | 对策略增益方向影响有限 |
| D7 | 对齐官方评测协议 | 正向 | 消除伪差异 |
| D8 | 多模态声学通道（扩展） | 新增 | v2 已修好：SER 0.174→0.930（同域） |

### 6.3 可复用工程经验

- **小模型做 Likert 评测**：greedy 解码会让 1.5B 模型恒吐同一数字，必须改为**首 token
  分布期望值**（或 few-shot），否则五种策略在数学上等价。
- **小模型做结构化情感打分**：让它输出 8 维 1–10 的 JSON 会全部退化到中性；改用**预训练
  情感分类器 + 维度映射**（或锚点余弦）更稳，且契约不变。
- ** transformers ≥5 + torch 2.5 加载 `.bin` 被拒**：用 `convert_to_safetensors.py` 一次性转
  safetensors 绕过（CVE-2025-32434），无需升级 torch。
- **HuggingFace 主站不可达时用 hf-mirror**，并自写**可续传的字节区间下载器**（本仓库
  `scripts/` 下一组 fetch 脚本），因为镜像会限流/断流。
- **本地 6 GB 显存跑 1.5B**：`PYTORCH_CUDA_ALLOC_CONF=expandable_segments:true` + 只算
  最后一位 logits（`logits_to_keep=1`），否则 lm_head 对全序列展开会 OOM。

### 6.4 局限与后续

- **骨干（仍未解决，量级最大的一项）**：若有 7B+/72B 或 API 主干，绝对精度与英文作答会大幅改善
  （预计接近论文）。本机 6 GB 显存，且 GPU 不稳定（同进程内连续前向约 6 次后硬崩），
  不足以稳定承载 7B 级别推理。
- **声学通道（本轮已修好，但留下口径问题）**：v2 换成 RAVDESS 8 类 SER 后准确率
  0.174 → 0.930，但该数字是**同域**评估（检查点本身在 RAVDESS 上微调）。要得到泛化结论，
  需要在**留出说话人（speaker-disjoint）**的划分上重测。
- **MM-2 跨通道一致性（已跑通，但结论偏负面）**：TTS 原依赖 `Add-Type -AssemblyName
  System.Speech`，被沙箱安全策略禁止；改用 pyttsx3（COM）+ 逐条子进程超时后已跑通。
  结果是在**中性 TTS** 下声学通道几乎无增量信息（Pearson ≈ 0，见 §5.3）——
  需要带情感的语音才能体现声学通道价值。
- **数据**：扩到 InCharacter 全部 32 角色、并补齐英文角色的可用作答，可让结论更稳健。

---

## 7. 本轮优化记录（v1 → v2）

| # | 优化项 | v1 状态 | v2 结果 |
|---|---|---|---|
| 1 | 声学 SER 检查点 | IEMOCAP **4 类** HuBERT 套 RAVDESS 8 类，恒预测 `anger`，acc **0.174** | 换 RAVDESS **8 类** SER，acc **0.930**（同域） |
| 2 | 分类头加载 | transformers 把 head 判为 MISSING 并**静默随机初始化**，输出实为噪声 | `_has_legacy_head()` 自动检测并重建 head；head 结构用真值**实测**选定 |
| 3 | Plutchik 映射覆盖 | 仅覆盖 neu/hap/sad/ang 四类 | 补齐全部 8 类（含 calm/neutral/fearful/disgust/surprised） |
| 4 | 仓库工程整洁度 | `scripts/` 21 个脚本（含 9 个冗余下载/探针） | 精简至 **12 个**，冗余移入 `scripts/_archive/`（本地保留、不发布） |
| 5 | 评测可用性 | 固定 CPU + 固定检查点 | 新增 `--ser` / `--device`；v1 失败可一键复现 |
| 6 | MM-2 跨通道一致性 | TTS 被沙箱 `Add-Type` 策略阻断，无法合成语音 | 改用 pyttsx3 + **逐条子进程超时**；已跑通（余弦 0.795/0.813，Pearson ≈ 0） |

### 仍然没做到的（如实列出）

1. **骨干升级**——6 GB 显存 + GPU 硬崩，7B 无法在本机稳定跑；绝对精度与英文退化问题未解决。
2. **声学同域问题**——0.930 尚未在 speaker-disjoint 划分上复测，不能当作泛化指标。
3. **MM-2 的增量信息**——虽已跑通，但结论是中性 TTS 下声学通道几乎无增量（Pearson ≈ 0）。
   要让融合真正增益，需要**带情感的 TTS 或真实角色语音**，而不是中性朗读。
4. **数据规模**——仍是 15 个角色子集（6 中文 + 9 英文），未扩到论文的全 32 角色。
